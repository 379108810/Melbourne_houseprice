from flask import Flask, render_template

app = Flask(__name__, template_folder='yourpath')

@app.route('/')
def hello():
    return render_template('House_Price_analysis_platform.html')  

if __name__ == '__main__':
    app.run(debug=True)

